#!/bin/bash

while true
do
	# choose a random wallpaper
	pape=$(ls ~/wallpapers | sort -R | tail -1)
	# set the random wallpaper
	hsetroot -cover ~/wallpapers/$pape
	# wait 3 minutes (180 seconds)
	sleep 180
done
